# DEV + CI tooling, pinned for reproducibility.
# Runtime deps are in app/requirements.txt
ruff==0.15.17
mypy==2.1.0
pytest==9.1.0
httpx==0.28.1 # ASGI transport behind the FastAPI TestClient in the API tests